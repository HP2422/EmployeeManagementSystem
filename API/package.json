{
  "name": "assignment_harshkumarpatel",
  "version": "1.0.0",
  "description": "\"Full-Stack Assignment\"",
  "main": "server.js",
  "scripts": {
    "compile": "babel src --out-dir public --presets @babel/react",
    "watch": "babel src --out-dir public --watch --verbose --presets @babel/react",
    "start": "nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "HARSH",
  "license": "ISC",
  "dependencies": {
    "apollo-server-express": "^3.11.1",
    "express": "^4.18.2",
    "graphql": "^15.8.0",
    "nodemon": "^2.0.20",
    "server.js": "^1.0.0"
  },
  "devDependencies": {
    "mongoose": "^6.9.1"
  }
}